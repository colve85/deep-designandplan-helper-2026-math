# 함께 만들기

수업 소재와 단원 예시가 늘어날수록 쓸모가 커지는 저장소입니다.
현장에서 써 보신 소재를 보태 주시면 좋겠습니다.

## 무엇을 보태면 좋은가

- **다른 과목·학년의 단원 자료** — 현재 수록 과목의 미수록 내용요소, 중학교 수학, 전문·고급 과목군
- **더 나은 수업 소재** — 실제로 해 보니 학생 반응이 좋았던 자료·맥락
- **오개념 목록** — 그 단원에서 학생이 실제로 자주 넘어지는 지점
- 화면·문서 생성의 버그 수정

## 작업 순서

1. 저장소를 포크하고 브랜치를 만듭니다.
2. 단원 자료는 [docs/data-guide.md](docs/data-guide.md)의 구조를 따릅니다.
3. 고친 뒤 반드시 점검을 돌립니다.

   ```bash
   node tools/verify.js
   ```

   화면을 고쳤다면 개발 서버로 직접 확인해 주세요.

   ```bash
   node tools/dev-server.js
   ```

4. Pull Request를 보냅니다. 어떤 단원을 어떤 이유로 더했는지 한두 줄이면 충분합니다.

## 글을 쓸 때의 기준

- 소재는 **구체적으로**. "실생활 상황"이 아니라 "학교 축제 부스 예산표".
- 목표는 **관찰 가능한 행동**으로. "잘 이해한다"가 아니라 "근거를 들어 설명한다".
- 증거는 **목표와 짝이 맞게**. 목표가 설명이면 증거도 설명하는 장면이어야 합니다.
- 특정 교과서·학습지·상업 자료를 그대로 옮기지 않습니다.

## 코드 스타일

- Apps Script V8이지만 `var` 와 `function` 을 씁니다 (기존 코드와 맞추기 위함).
- 내부 전용 함수는 이름 끝에 `_` 를 붙입니다 (Apps Script 규칙).
- 주석과 화면 문구는 우리말로 씁니다.
